# name: common-widget
# --

import 'package:common/common.dart';


import '`(string-inflection-underscore-function (string-replace ".dart" "" (buffer-name)))`_controller.dart';

class `(string-inflection-upper-camelcase-function (string-replace ".dart" "" (buffer-name)))` extends GetWidget {
  const `(string-inflection-upper-camelcase-function (string-replace ".dart" "" (buffer-name)))`({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}