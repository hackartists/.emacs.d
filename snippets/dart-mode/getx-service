# -*- mode: snippet -*-
#name : getx-service
#key : #gxs
# --


import 'package:get/get.dart';

// add below code to main.dart
// await Get.putAsync(() => `(string-inflection-upper-camelcase-function (string-replace ".dart" "" (buffer-name)))`().init());
class `(string-inflection-upper-camelcase-function (string-replace ".dart" "" (buffer-name)))` extends GetxService {
  Future<`(string-inflection-upper-camelcase-function (string-replace ".dart" "" (buffer-name)))` > init() async {
    $0

    return this;
  }
}
