# -*- mode: snippet -*-
#name : getx-service
#key : #gxs
# --


import 'package:get/get.dart';

// add below code to main.dart
class `(string-inflection-upper-camelcase-function (string-replace ".dart" "" (buffer-name)))` extends GetxService {
  static void init()  {
    Get.put<`(string-inflection-upper-camelcase-function (string-replace ".dart" "" (buffer-name)))`>(`(string-inflection-upper-camelcase-function (string-replace ".dart" "" (buffer-name)))`());

    return this;
  }
}
