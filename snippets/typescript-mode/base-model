# name: base-model
# --

import { createModel } from '@wb/libs';

export interface I${1:Name}Model {
  ${2:field}: string;
}

export const $1Model = createModel<I$1Model>('$1', {
  $2: {
    type: String,
    required: true,
    unique: true,
    index: true,
  },
});