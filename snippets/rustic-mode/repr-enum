# -*- mode: snippet -*-
# name: repr-enum
# --

use crate::*;

#[derive(
    Debug,
    Clone,
    Copy,
    PartialEq,
    Eq,
    serde_repr::Serialize_repr,
    serde_repr::Deserialize_repr,
    Default,
    DynamoEnum,
    JsonSchema_repr,
)]
#[repr(u8)]
pub enum ${1:EnumName} {
    #[default]
    ${2:VarName} = 1,
}
