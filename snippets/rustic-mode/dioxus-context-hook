# -*- mode: snippet -*-
#name : dioxus-context-hook
#key : dioxus-context-hook
#contributor : hackartist
# --

use crate::*;

#[derive(Clone, Copy, DioxusController)]
pub struct `(string-inflection-upper-camelcase-function (file-name-base))` {
pub data: Loader<_>
}

#[track_caller]
pub fn `(string-inflection-snake-case-function (file-name-base))`() -> `(string-inflection-upper-camelcase-function (file-name-base))` {
use_context()
}

#[track_caller]
pub fn `(string-inflection-snake-case-function (file-name-base))`_provider() -> Result<`(string-inflection-upper-camelcase-function (file-name-base))`, Loading> {
    let data = use_loader(move || async move {
          // TODO: loading data
    })?;

    let ctx = use_context_provider(move || {
      `(string-inflection-upper-camelcase-function (file-name-base))` {
        data,
      }
    });

    Ok(ctx)
}