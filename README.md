pkTo enable python development environment correctly,

You run "M-x" in Emacs. And then type "elpy-config" for the status of your python.

After that, you should install python packages with pip or ease_install.
